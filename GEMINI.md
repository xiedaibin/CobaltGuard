# Cobalt Price Monitor & Alert System (CobaltGuard)

## 1. 系统概述
CobaltGuard 是一个自动化的金属钴价格监测系统，旨在从生意社（100ppi.com）抓取每日价格，提供数据持久化、波动预警（飞书推送）、定期报表（带折线图）以及 RESTful API 接口。

## 2. 技术架构

### 2.1 技术栈
- **Web 框架**: FastAPI (Python 3.10+)
- **数据库**: SQLite (轻量级，适合单机运行)
- **ORM**: SQLAlchemy
- **任务调度**: APScheduler (内置于应用进程)
- **爬虫技术**: Requests + BeautifulSoup4
- **可视化**: Matplotlib (生成趋势折线图)
- **通知系统**: Feishu (Lark) Webhook
- **部署**: Docker + Docker Compose

### 2.2 核心模块
1. **Scraper (爬虫模块)**: 负责解析 HTML，提取日期和钴价格，包含异常重试机制。
2. **Database (数据模块)**: 管理 SQLite 连接与模型定义（CobaltPrice）。
3. **Strategy (策略引擎)**: 计算价格波动率，触发 5% 预警逻辑。
4. **Reporter (报表引擎)**: 每日 9:00 生成汇总数据和折线图。
5. **API (接口模块)**: 提供查询接口和手动触发接口。
6. **Notifier (通知模块)**: 封装飞书消息发送逻辑，支持富媒体（图片）。

### 2.3 数据流向
1. `APScheduler` 定时触发 `Scraper`。
2. `Scraper` 抓取数据并存入 `SQLite`。
3. `Strategy` 对比历史数据（30天前），若波动 > 5%，调用 `Notifier` 发送实时预警。
4. 每天 9:00，`Reporter` 生成周报图表，调用 `Notifier` 发送。
5. 用户通过 `FastAPI` Swagger UI 或 API 调用查询数据。

## 3. 部署架构 (Docker)
- **镜像**: 基于 `python:3.10-slim`，安装中文字体（wqy-zenhei）以支持 Matplotlib 中文显示。
- **持久化**: 挂载 `/app/data` 目录到主机，确保存储的 SQLite 数据库文件不因容器重启丢失。
- **时区**: 强制设置为 `Asia/Shanghai`。

## 4. 安全与扩展性
- **重试机制**: 爬虫失败后按指数退避算法重试。
- **配置管理**: 使用 `.env` 文件或环境变量管理 Feishu Webhook URL。
- **解耦设计**: 采集任务与 API 服务解耦，即使采集失败不影响 API 查询。

## 5. AI 交互约定
- **语言**: 与用户的任何交互必须全程使用**中文**进行。
- **主动执行决策**: 作为高级架构师和研发角色，关于代码调试、报错排查、测试验证等具体操作，如果不需要产品层面的重大业务决策，AI 均视为“默认同意”需要自行判断并优先直接执行解决问题。只有遇到核心架构变动、关键业务逻辑或不可逆破坏性操作时，才需要向用户请示并要求人工干预。
- **严谨验证作风**: 在完成功能开发、Bug 修复或配置修改后，必须自动执行独立测试、读取日志、进行视觉验证等实质性检查操作。只有在“实锤”测试通过后，才能向用户汇报结果，绝对杜绝不经实地检验就凭空断言“已经修好”的作风。
- **注释规范**: 所有代码中的注释、Docstrings 以及提示信息必须统一使用**中文**，以确保国内团队维护的高效性。
